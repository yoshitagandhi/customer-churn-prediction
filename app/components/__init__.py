"""Reusable Streamlit UI components: sidebar, forms, cards, charts, metrics, tables.

Each component takes already-computed data and renders it — none of
them compute predictions, explanations, or recommendations
themselves. That logic lives in `app/services/`.
"""
