"""Streamlit application package: the project's presentation layer.

Run with `streamlit run app/app.py`. This package contains no
machine-learning logic itself -- it orchestrates calls into `src/`
(via `app/services/`) and renders results with Streamlit widgets
(via `app/components/` and `app/pages/`).
"""
